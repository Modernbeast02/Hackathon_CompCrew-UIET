// import Typed from 'typed.js';

var options = {
    strings: ["The Best Doctor", "Philanthropist", "Dumb"],
    typeSpeed: 40,
    backspeed: 30,
    loop: true,
}

var typed = new Typed(".moving-text", options);
